import { CreateCelebration } from "@/components/create/CreateCelebration";

export const metadata = {
  title: "Create a celebration · Owambe",
  description: "Set up a celebration and invite people to give from anywhere.",
};

export default function CreatePage() {
  return <CreateCelebration />;
}
